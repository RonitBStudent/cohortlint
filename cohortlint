#!/usr/bin/env python3
"""Run CohortLint from a source checkout without installation."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent / "src"))

from cohortlint.cli import main  # noqa: E402


raise SystemExit(main())
